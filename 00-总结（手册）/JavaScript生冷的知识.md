//arguments.length 
把 return arguments.length ; 放到函数里面
返回函数调用过程接收到的参数个数


//toString()
调用  方法 将 函数 作为一个字符串返回:
如 xxg.toString()


//argument
用来抓取 function xxg(很多个参数) 指定一个的值
如: arguments[0] 抓取第0个 可以用for和arguments来遍历这些个参数

//写到控制台:
console.log();